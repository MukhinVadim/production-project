import { Theme } from 'shared/lib/theme-provider/ThemeContext';
import { useTheme } from 'shared/lib/theme-provider/useTheme';
import { ThemeProvider } from './ThemeProvider';

export { useTheme, ThemeProvider, Theme };
